{
	"hero": {
		"kicker": "THE TICKET RAIL — TASK STATE",
		"title": "Durable task state for AI agents",
		"sub": "A task tracker for AI agents, shipped as one Zig binary with SQLite built in. It serves a JSON REST API on 127.0.0.1: agents claim tasks by role under expiring leases, report progress, and move tasks through declared stages — so long-running work survives crashes and restarts without a human nudging it along."
	},
	"metrics": [
		{
			"value": "33",
			"label": "REST endpoints, OpenAPI 3.1"
		},
		{
			"value": "1.3–1.7 MB",
			"label": "full release binary, SQLite inside"
		},
		{
			"value": "7",
			"label": "prebuilt targets, incl. riscv64"
		},
		{
			"value": "FTS5",
			"label": "full-text search over shared key-value store"
		}
	],
	"install": {
		"primary": {
			"label": "Release binary",
			"code": "curl -LO https://github.com/nullclaw/nulltickets/releases/download/v2026.5.29/nulltickets-linux-x86_64.bin\nchmod +x nulltickets-linux-x86_64.bin\n./nulltickets-linux-x86_64.bin --port 7700 --db tracker.db"
		},
		"alts": [
			{
				"label": "Build from source",
				"code": "git clone https://github.com/nullclaw/nulltickets\ncd nulltickets\nzig build\nzig build run -- --port 7700 --db tracker.db"
			},
			{
				"label": "Via NullHub",
				"code": "nullhub install nulltickets"
			}
		]
	},
	"quickstart": "Start the tracker, then talk to it over plain JSON/HTTP.\n\n```bash\n# start the server (defaults: --port 7700, --db nulltickets.db)\nzig build run -- --port 7700 --db tracker.db\n\n# check service and queue health\ncurl http://127.0.0.1:7700/health\n\n# fetch the OpenAPI 3.1 schema agents bootstrap from\ncurl http://127.0.0.1:7700/openapi.json\n\n# run the test suite\nzig build test\n```",
	"features": [
		{
			"title": "Lease-based claiming",
			"body": "An agent asks for the next task matching its role (POST /leases/claim) and gets it with a time-limited lease — 5 minutes by default, extended by heartbeats; if the agent crashes, the lease expires and the task becomes claimable again. Claiming is a single SQL query covering role match, dependency resolution, retry eligibility and active leases: a task waits until everything it depends on is finished, or until the one agent it is pinned to asks. Lease tokens are returned once and stored only as SHA-256 hashes."
		},
		{
			"title": "Pipeline state machines",
			"body": "You declare which stages a task can be in and which moves between them are allowed — e.g. plan → review → done. Definitions are validated at creation (eight distinct error cases), the server rejects any transition that breaks the rules, and optional expected_stage / expected_task_version checks return typed 409s so two agents can't overwrite each other's updates."
		},
		{
			"title": "Safe retries with Idempotency-Key",
			"body": "Every write accepts an Idempotency-Key header. The server stores a SHA-256 hash of the request body per key: replaying the same request returns the cached response, and reusing a key with a different body gets 409 idempotency_conflict. An agent on a flaky connection can retry any write without double-creating tasks."
		},
		{
			"title": "Retries and dead letters",
			"body": "Per-task retry policy: max_attempts, a retry_delay_ms backoff before a failed task becomes claimable again, and an optional dead_letter_stage that catches exhausted tasks — with the failure reason recorded — instead of losing them. Failure handling runs inside a single SQLite transaction."
		},
		{
			"title": "Shared agent memory",
			"body": "A namespaced key-value store (PUT /store/{namespace}/{key}) with SQLite FTS5 full-text search and exact JSON filtering gives agents durable memory across sessions."
		},
		{
			"title": "Self-describing and observable",
			"body": "GET /openapi.json (also at /.well-known/openapi.json) returns the full OpenAPI 3.1 schema embedded in the binary, so an agent can discover every endpoint at runtime instead of shipping a hand-written client. The server also ingests OpenTelemetry (OTLP) traces, linking spans to tasks and runs, and GET /ops/queue reports stuck tasks and near-expiry leases for orchestrators."
		}
	],
	"pairs": [
		"claw",
		"boiler",
		"hub"
	],
	"note": "Pre-1.0: the API is documented and e2e-tested, but config and CLI may change between releases. The server binds 127.0.0.1 only and handles one connection at a time — a local coordinator by design; put a reverse proxy in front if remote agents need it. Known quirk in v2026.5.29: --version and GET /health still report the stale version string 2026.3.2.",
	"plain": {
		"what": "A single-binary HTTP service (written in Zig, SQLite embedded) that stores tasks for AI agents and hands them out through a JSON REST API on 127.0.0.1 — agents claim work by role, report progress events, and move tasks through defined stages.",
		"why": "Long-running agent work normally dies with the process that runs it; NullTickets keeps the backlog, retries, and full history in SQLite on disk, so a crashed or restarted agent loop picks up where it left off instead of needing a human to restart it.",
		"when": "You have a backlog too big for one session — say 100 tasks loaded in one POST /tasks/bulk call — and want an agent loop (nullclaw, the companion agent runtime, or any runtime that implements the claim → events → transition contract) to grind through it unattended, with failed tasks retried automatically."
	}
}
